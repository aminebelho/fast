import './globals.css';

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
    <head>
      <title>Test Fast Delivery</title>
    </head>
    <body>{children}</body>
  </html>
  );
}
